#include "sense_avoid.h"

/* Finds next state */
enum state state_machine(enum state current_state, enum state next_state) {
    /* https://www.codeproject.com/Articles/1275479/State-Machine-Design-in-C 
    https://yakking.branchable.com/posts/state-machines-in-c/ */
    enum state newState = NORMAL;
    return newState;
}